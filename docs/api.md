# API Documentation

This project uses `@elysiajs/openapi` for internal API documentation.

## Accessing the Documentation

Interactive API documentation is available in development mode only at `<BACKEND_URL>/openapi` after you started the backend server, for example:

```
http://localhost:8080/openapi
```

It contains a complete list of all internal API endpoints and interactive testing interfaces.
